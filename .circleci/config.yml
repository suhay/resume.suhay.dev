version: 2.1
orbs:
  node: circleci/node@4.1.0

jobs:
  build:
    executor:
      name: node/default
      tag: '13'
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run:
          name: Gatsby build
          command: yarn build
      - run:
          name: Zip up site
          command: |
            echo "Saving file ${CIRCLE_SHA1}.tar.gz"
            tar -zcvf ${CIRCLE_SHA1}.tar.gz public
      - store_artifacts:
          path: ${CIRCLE_SHA1}.tar.gz

  deploy:
    machine:
      enabled: true
    steps:
      - run:
          name: SCP artifact
          command: scp ${CIRCLE_SHA1}.tar.gz ubuntu@suhay.dev:/mnt/projects/resume.suhay.dev/${CIRCLE_SHA1}.tar.gz

workflows:
  build_and_deploy:
    jobs:
      - build




      
